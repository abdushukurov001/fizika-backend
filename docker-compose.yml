version: '3.9'
name: 'fizika'

networks:
  dev:

x-base-api: &base-project
  env_file:
    - .env
  build:
    context: .
    dockerfile: Dockerfile
  volumes:
    - ./:/opt/app

services:
  project:
    <<: *base-project
    command: ["make", "run"]
    depends_on:
      - postgres-db
    ports:
      - ${SERVER_PORT:-8000}:8000
    volumes:
      - ./:/opt/app
      - ./static:/opt/app/static
      - ./media:/opt/app/media
    networks:
      - dev
  nginx:
    image: nginx:latest
    container_name: fizika_nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./media:/opt/app/media
      - ./static:/opt/app/static
      - /etc/letsencrypt:/etc/letsencrypt:ro
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - project
    networks:
      - dev

  postgres-db:
    env_file:
      - .env
    image: postgres:15.0-alpine
    volumes:
      - pg_data:/var/lib/postgresql/data:rw
    networks:
      - dev

volumes:
  static_volume:
  media_volume:
  pg_data:
